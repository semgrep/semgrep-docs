---
title: "Semgrep Guardian rules and configuration"
description: "Which rules Semgrep Guardian scans with, and how that differs between the remote and local CLI setups."
sidebarTitle: Rules and configuration
---

Guardian uses different rules depending on the environment in which you use it. This is one of the main practical differences between the two setup paths, so check which one applies to you.

## Which rules run

<Tabs>
  <Tab title="Claude Code remote server (default)">
    Scans with the default [Guardian ruleset](https://semgrep.dev/p/guardian-default).

    This ruleset is not currently configurable. If you require custom rules, see Semgrep's `#mcp` [Slack community](https://go.semgrep.dev/slack) for assistance, or use a local CLI setup instead.
  </Tab>
  <Tab title="Claude Code local, and all other agents">
    Scans with the rules enabled for your Semgrep organization through your [Policies](/semgrep-code/policies).

    Because these setups use your organization's Policies, changes you make there apply to Guardian scans as well.
  </Tab>
</Tabs>

## What Guardian scans for

Guardian scans generated files using Semgrep Code, Supply Chain, and Secrets, so findings can cover:

* Code vulnerabilities detected by [Semgrep Code](/semgrep-code/overview)
* Vulnerable dependencies detected by [Semgrep Supply Chain](/semgrep-supply-chain/overview)
* Committed credentials detected by [Semgrep Secrets](/semgrep-secrets/conceptual-overview)

## Related pages

* [Choose your setup](/semgrep-guardian/choose-your-setup)
* [Authentication](/semgrep-guardian/authentication)
