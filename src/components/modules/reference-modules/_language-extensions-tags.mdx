<!--- The table in this module is mainly used in rule-syntax documentation.
To update supported languages, edit _supported-languages-table.mdx in the same
directory.
To make the table hidden by default and only show it after a user expands it,
use (either around the import of this module in a specific file
or by default) uncomment the following two lines and closing tag below the table: -->
<!-- <details> -->
<!-- <summary>Expand table of extensions and tags</summary> -->

| Language   | Extensions             | Tags                                 |
|:-----------|:-----------------------|:-------------------------------------|
| Bash       | `.sh`                  | `bash`                               |
| C          | `.c`                   | `c`                                  |
| C++        | `.cpp`, `.h`           | `cpp`                                |
| C#         | `.cs`                  | `csharp`, `cs`, `C#`                 |
| Generic    |                        | `generic`                            |
| Go         | `.go`                  | `go`, `golang`                       |
| Hack       | `.h`, `.hack`          | `hack`                               |
| HTML       | `.htm`, `.html`        | `html`                               |
| Java       | `.java`                | `java`                               |
| JavaScript | `.js`, `.jsx`          | `js`, `jsx`, `javascript`            |
| JSON       | `.json`                | `json`, `JSON`, `Json`               |
| JSX        | `.js`, `.jsx`          | `js`, `jsx`, `javascript`            |
| Kotlin     |  `.kt`, `.kts`, `.ktm` | `kotlin`                             |
| Lua        | `.lua`                 | `lua`                                |
| OCaml      | `.ml`, `.mli`          | `ocaml`, `ml`                        |
| PHP        | `.php`                 | `php`                                |
| Python     | `.py`, `.pyi`          | `python`, `python2`, `python3`, `py` |
| R          | `.r`, `.rda`, `rds`    | `r`                                  |
| Ruby       | `.rb`                  | `ruby`, `rb`                         |
| Rust       | `.rs`                  | `rust`, `Rust`, `rs`                 |
| Scala      | `.scala`, `.sc`        | `scala`                              |
| Solidity   | `.sol`                 | `solidity`                           |
| Terraform  | `.tf`                  | `hcl`                                |
| TypeScript | `.ts`, `.tsx`          | `ts`, `tsx`, `typescript`            |
| TSX        | `.ts`, `.tsx`          | `ts`, `tsx`, `typescript`            |
| YAML       | `.yaml`                | `yaml`                               |

<!-- </details> -->

<!-- imports: This file is currently imported to: rule-syntax.md -->